{
    VAR w = 0, 
    
    PROC soma (INT x, INT y, name INT z) {
        z := x + y
    };
        WRITE(w);           // 1. Estado inicial de w (deve imprimir 0)
        CALL soma(2, 3, w); // 2. Chama o procedimento passando w por nome
        WRITE(w)            // 3. Verifica se w foi alterado (deve imprimir 5)
}